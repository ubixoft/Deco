{
  "name": "server",
  "description": "Deco MCP server with an Astro documentation site, tools and workflows",
  "version": "1.0.0",
  "scripts": {
    "dev": "deco dev --clean-build-dir ./view-build",
    "deploy": "wrangler build && deco deploy ./dist -y -f -t $DECO_DEPLOY_TOKEN",
    "build": "wrangler build",
    "gen": "deco gen > deco.gen.ts"
  },
  "dependencies": {
    "@deco/workers-runtime": "npm:@jsr/deco__workers-runtime@0.20.0",
    "zod": "4.1.11"
  },
  "devDependencies": {
    "deco-cli": "^0.18.1"
  }
}
