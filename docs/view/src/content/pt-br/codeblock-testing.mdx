---
title: Teste de CodeBlock
description: Página de teste para o componente CodeBlock com todos os recursos
icon: Code
---

Esta página testa todos os recursos do nosso componente CodeBlock aprimorado usando sintaxe meta markdown adequada com o plugin rehype-mdx-code-props.

## Teste Simples (deve mostrar nome do arquivo no console)

```javascript filename="test.js"
console.log("Hello World");
```

## Bloco de Código Básico

```javascript
const greeting = "Hello, World!";
console.log(greeting);
```

## Bloco de Código com Título

```javascript title="hello.js"
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```

## Bloco de Código com Nome do Arquivo

```javascript filename="hello.js"
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```

## Bloco de Código com Linhas

```javascript lines
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```

## Bloco de Código com Ícone

```javascript icon="Zap"
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```

## Bloco de Código com Quebra de Texto

```javascript wrap
const veryLongVariableName =
  "This is a very long string that would normally cause horizontal scrolling but with wrap enabled it should break to new lines instead";
```

## Bloco de Código Expansível

```javascript expandable
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
function sayGoodbye() {
  console.log("Goodbye, World!");
}
function calculateSum(a, b) {
  return a + b;
}
function calculateProduct(a, b) {
  return a * b;
}
sayHello();
sayGoodbye();
console.log(calculateSum(5, 3));
console.log(calculateProduct(4, 7));
```

## Exemplo Completo com Todos os Recursos

```javascript title="example.js" lines expandable icon="Arrow" wrap
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
function sayGoodbye() {
  console.log("Goodbye, World!");
}
function calculateSum(a, b) {
  return a + b;
}
function calculateProduct(a, b) {
  return a * b;
}
function performComplexCalculation() {
  const veryLongVariableName =
    "This is a very long string that would normally cause horizontal scrolling but with wrap enabled it should break to new lines instead";
  console.log(veryLongVariableName);
  return calculateSum(calculateProduct(2, 3), calculateProduct(4, 5));
}
sayHello();
sayGoodbye();
console.log(calculateSum(5, 3));
console.log(calculateProduct(4, 7));
console.log(performComplexCalculation());
```

## Linguagens Diferentes

### Exemplo Python

```python filename="hello.py" lines icon="FileText"
def hello_world():
    print("Hello, World!")

def calculate_sum(a, b):
    return a + b

hello_world()
result = calculate_sum(5, 3)
print(f"Sum: {result}")
```

### Exemplo TypeScript

```typescript filename="types.ts" lines expandable
interface User {
  id: number;
  name: string;
  email: string;
}

class UserService {
  private users: User[] = [];

  addUser(user: User): void {
    this.users.push(user);
  }

  getUserById(id: number): User | undefined {
    return this.users.find((user) => user.id === id);
  }

  getAllUsers(): User[] {
    return this.users;
  }
}

const userService = new UserService();
userService.addUser({ id: 1, name: "John Doe", email: "john@example.com" });
```

### Exemplo Bash

```bash filename="deploy.sh" icon="Terminal"
#!/bin/bash
npm install
npm run build
npm run deploy
echo "Deployment complete!"
```

### Java com Linhas

```java lines
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```
