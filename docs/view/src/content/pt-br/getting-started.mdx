---
title: Começando
description: Crie e implante seu primeiro servidor MCP em minutos
icon: Play
---

import { StepGroup } from "../../components/ui/StepGroup";
import { Step } from "../../components/ui/Step";
import Callout from "../../components/ui/Callout.astro";

## Pré-requisitos

Antes de começar, certifique-se de ter:

- **Node.js** v22+ ([baixar aqui](https://nodejs.org))
- **Git** para controle de versão
- **Editor de código** (recomendamos [Cursor](https://cursor.com) para desenvolvimento com IA)
- **Conhecimento básico de TypeScript** – O projeto é pesado em TypeScript, então familiaridade com TS é importante (se você é novo, veja a documentação oficial do TypeScript para uma introdução).

## Criando um novo App

<StepGroup>
  <Step number={1} title="Inicializar seu app">
    ```bash
    npm create deco@latest
    ```
    
    Isso pode abrir seu navegador para autenticar. Você pode criar uma
    nova conta se não tiver uma.
    
    Você será solicitado a vincular este app a um projeto deco CMS
    do qual você faz parte.
  </Step>

  <Step number={2} title="Iniciar desenvolvimento">
    ```bash
    cd <nome-do-seu-app>
    npm install
    npm run dev
    ```
    
    O servidor local do seu app iniciará com hot reload habilitado.
    Copie a URL de preview da saída do terminal.
    A URL de preview é acessível de qualquer lugar na internet agora.
    
    <Callout type="info">
      O servidor local pode ser acessado na porta 8787 e serve
      tanto um app MCP em `/mcp`, quanto um front-end React em `/`.
    </Callout>
  </Step>
</StepGroup>

## Teste seu MCP

Teste seu servidor MCP:

1. Vá para sua equipe no [deco CMS](https://admin.decocms.com)
2. Clique em **Adicionar Integração** → **Integração Customizada**
3. Cole sua URL de preview + `/mcp` (ex., `https://localhost-abc123.deco.host/mcp`)
4. Salve e teste as ferramentas padrão que vêm do template

<Callout type="success">
  Seu servidor MCP agora está rodando localmente e conectado ao deco! 🎉
</Callout>

## Implantar em produção

Quando estiver pronto para compartilhar seu MCP:

```bash
npm run deploy
```

Implantamos seu app nos Cloudflare Workers e ele fica disponível no registro privado da sua Equipe.